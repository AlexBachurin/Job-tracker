import React from "react";
import Wrapper from "../assets/wrappers/StatsItem/StatsItemWrapper";
const StatsItem = ({ title, count, icon, color, bcg }) => {
	return <Wrapper>StatsItem</Wrapper>;
};

export default StatsItem;
